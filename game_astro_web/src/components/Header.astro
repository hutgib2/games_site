---
import { Menu, Select } from 'webcoreui/astro';
import DupaLogo from '../assets/my_logo.png';
// import { gameData } from '../lib/gameData';

const menu = [
	{ name: 'Home', href: '/', active: true },
	{ name: 'About', href: '/about', active: true },
];

const menuLogo = {
	url: DupaLogo.src,
	width: 64,
};
---

<header>
	<div><Menu items={menu} logo={menuLogo} /></div>
	<div>
<<<<<<< HEAD
		<!-- <Select
		name="select-id"
		itemGroups={[{
			items: 
			Object.values(gameData).map((game) => ({
				name: game.title,
				href: game.href,
			}))
		}]}
		showSearchBar={false}
		showSearchBarIcon={false}
		placeholder="Games"
		/> -->
=======
		<Select
			name="select-id"
			itemGroups={[
				{
					items: Object.values(gameData).map((game) => ({
						name: game.title,
						href: game.href,
					})),
				},
			]}
			showSearchBar={false}
			showSearchBarIcon={false}
			placeholder="Games"
		/>
>>>>>>> 42743a58ab8446f1e09820eedfa5d0c9c2043413
	</div>
</header>

<!-- <script>
    import { listen } from 'webcoreui'

    listen('selectOnChange', (event) => console.log(event))
    listen('selectOnClose', (event) => console.log(event))
</script> -->

<style>
	header {
		display: flex;
		flex-direction: row;
		align-items: center;
	}
</style>
